# Environment Variables for Multi-Task Text Utility
# Copy this file to .env and fill in your actual values

# Prompt Template Selection (Optional)
# Specify which prompt template file to use (must be in prompts/ directory)
# Default: main_prompt.txt
# Available prompts:
#   - main_prompt.txt (default) - Comprehensive customer support assistant
#   - technical_prompt.txt - Technical support specialist, focused on troubleshooting
#   - concise_prompt.txt - Brief, direct responses for quick answers
# Examples:
#   PROMPT_FILE=technical_prompt.txt
#   PROMPT_FILE=concise_prompt.txt
PROMPT_FILE=main_prompt.txt

# Provider Priority (Optional)
# If NOT set: Uses default priority (openrouter,gemini,openai) - all three providers
# If SET: Only uses the providers specified (others will be ignored)
# Valid values: openrouter, gemini, openai
# Examples:
#   PROVIDER_PRIORITY=openai                    → Only use OpenAI
#   PROVIDER_PRIORITY=gemini,openrouter         → Use Gemini first, then OpenRouter (OpenAI ignored)
#   PROVIDER_PRIORITY=openrouter,gemini,openai  → Use all three in custom order
# Leave empty or remove to use default (all three)
PROVIDER_PRIORITY=openrouter,gemini,openai

# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here
# Optional: Specify which OpenAI model to use (default: gpt-3.5-turbo)
OPENAI_MODEL=gpt-3.5-turbo

# Google Gemini API Configuration
GEMINI_API_KEY=your_gemini_api_key_here
# Optional: Specify which Gemini model to use (default: gemini-2.5-flash)
GEMINI_MODEL=gemini-2.5-flash

# OpenRouter API Configuration
OPENROUTER_API_KEY=your_openrouter_api_key_here
# Optional: Specify which model to use (default: openai/gpt-3.5-turbo)
# Examples: openai/gpt-3.5-turbo, openai/gpt-4, google/gemini-pro, anthropic/claude-3-opus
OPENROUTER_MODEL=openai/gpt-3.5-turbo
